{
  "name": "rishartinventory",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cd next-dashboard && npm run dev",
    "build": "cd next-dashboard && npm install && npm run build",
    "start": "cd next-dashboard && npm start",
    "all": "cd next-dashboard && npm install && npm run build && npm start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
